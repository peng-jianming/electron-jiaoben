# 在 python 目录下执行: python -m pip install -r requirements.txt
# 请使用已安装的 Python 3（勿使用 Microsoft Store 未完成的占位 python.exe）

numpy>=1.23
Pillow>=9.0
opencv-python>=4.5
# 无窗口 scrcpy 视频流（与 numpy/av/adbutils 版本绑定较严，若与 paddle 冲突请用独立 venv）
scrcpy-core==0.0.7
python-socketio>=5.0
paddlepaddle>=2.5.0
paddleocr>=2.7.0
ultralytics>=8.0
