import random

import ADB as adb

adb = adb.ADB控制器类()
print(adb.获取屏幕分辨率())


def 模拟人为向上滑动():
    """
    基于屏幕分辨率，进行一次拟人化的向上滑动
    参考 JS 中 随机ADB滑动 / 获取滑动坐标 的思路，增加一定随机偏移
    """
    分辨率 = adb.获取屏幕分辨率()
    if not 分辨率:
        print("无法获取屏幕分辨率，取消滑动")
        return

    宽, 高 = 分辨率

    # 定义“区域数组”而不是固定坐标：
    # 起始区域：屏幕中下方的一条横带 [x, y, width, height]
    起始区域 = [宽 * 0.3, 高 * 0.65, 宽 * 0.4, 高 * 0.1]

    # 结束区域：屏幕中上方的一条横带 [x, y, width, height]
    结束区域 = [宽 * 0.3, 高 * 0.25, 宽 * 0.4, 高 * 0.1]

    # 由 ADB 内部在两个区域数组内随机起点和终点，再做拟人化滑动
    adb.拟人滑动_区域([1566,274,81,38], [1571,341,69,38])


if __name__ == "__main__":
    # 示例：执行一次拟人化向上滑动
    for i in range(10):
        模拟人为向上滑动()